Get admin data by admin_id using auth super admin
---

tags:
  - Admin
security:
  - BasicAuth: []
parameters:
  - name: admin_id
    description: path should contain admin_id
    in: path
    required: true
    schema:
      type: integer
      required:
        - "admin_id"
      properties:
        admin_id:
          example: 1

responses:
  200:
    description: Successfully get data
  404:
    description: Item not found
  500:
    description: Internal server error