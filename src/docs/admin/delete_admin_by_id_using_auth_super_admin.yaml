Delete admin data by admin_id using auth super admin
---

tags:
  - Admin
security:
  - BasicAuth: []
parameters:
  - name: admin_id
    description: path should contain admin_id
    in: path
    required: true
    schema:
      type: integer
      required:
        - "admin_id"
      properties:
        admin_id:
          example: 1

responses:
  204:
    description: Successfully delete data
  404:
    description: Item not found
  400:
    description: Bad request, violates integrity constraints
  500:
    description: Internal server error