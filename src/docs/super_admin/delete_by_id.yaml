tags:
  - Super Admin
parameters:
  - name: id
    description: path should contain super admin ID
    in: path
    required: true
    schema:
      type: integer
      required:
        - "id"
      properties:
        id:
          example: 1

responses:
  204:
    description: Successfully delete data
  404:
    description: Super admin ID not found