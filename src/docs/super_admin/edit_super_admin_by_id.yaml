Edit super admin data by id
---

tags:
  - Super Admin
parameters:
  - name: body
    description: the body should contain name, email & password
    in: body
    required: true
    schema:
      type: object
      required:
        - "name"
        - "email"
        - "password"
      properties:
        name:
          type: "string"
          example: "super_admin"
        email:
          type: "email"
          example: "super_admin@gmail.com"
        password:
          type: "string"
          format: password
          example: "********"
  - name: id
    description: the path should contain super admin ID
    in: path
    required: true
    schema:
      type: integer

responses:
  200:
    description: Successfully insert data
  404:
    description: Super admin ID not found