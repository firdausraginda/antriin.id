Delete super admin data by super_admin_id
---

tags:
  - Super Admin
parameters:
  - name: super_admin_id
    description: path should contain super_admin_id
    in: path
    required: true
    schema:
      type: integer
      required:
        - "super_admin_id"
      properties:
        super_admin_id:
          example: 1

responses:
  204:
    description: Successfully delete data
  404:
    description: Item not found
  400:
    description: Bad request, violates integriy constraints
  500:
    description: Internal server error