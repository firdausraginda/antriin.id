Get user data by id using auth admin
---

tags:
  - User
security:
  - BasicAuth: []
parameters:
  - name: id
    description: path should contain user ID
    in: path
    required: true
    schema:
      type: integer
      required:
        - "id"
      properties:
        id:
          example: 1

responses:
  200:
    description: Successfully get data
  404:
    description: User or queue_user ID not found